# Alibaba Cloud Deployment Notes

For the final submission, deploy the API/dashboard to Alibaba Cloud and include:

- Public URL for `/health`
- Public URL for the dashboard `/`
- Public URL for `/runs/latest` after an agent run
- Proof run via `POST /autopilot/run?mock=false` when Qwen credentials are configured
- Screenshot of service dashboard
- Link to `agent/qwen_client.py` showing Qwen Cloud API usage
- Architecture diagram in `docs/architecture.md`
